PROJECT_NAME = "CryptoPilot AI"

INITIAL_CAPITAL = 200
MONTHLY_INVESTMENT = 100

DEFAULT_SYMBOL = "BTCUSDT"
DEFAULT_INTERVAL = "1h"

RISK_PER_TRADE = 0.02

DATABASE_PATH = "database/cryptopilot.db"
PORTFOLIO_STATE_PATH = "database/portfolio_state.json"

REPORT_PATH = "reports/"
LOG_PATH = "logs/"

# دارایی‌های پرتفوی و نماد معاملاتی هرکدام در بایننس
ASSET_SYMBOLS = {
    "BTC": "BTCUSDT",
    "ETH": "ETHUSDT",
    "LINK": "LINKUSDT",
    "ONDO": "ONDOUSDT",
    "NEAR": "NEARUSDT",
}

# ---------------------------------------------------------
# اسکنر فرصت‌های Long/Short بازار فیوچرز
# ---------------------------------------------------------

FUTURES_BASE_URL = "https://fapi.binance.com"

# چند ارز پرحجم فیوچرز اسکن شود
SCAN_TOP_N = 30

# تایم‌فریم کندل‌ها برای تحلیل اسکنر
SCAN_INTERVAL = "1h"

# درصد ریسک هر معامله فیوچرز از کل سرمایه (خیلی محافظه‌کارانه نگه داشته شده)
RISK_PER_TRADE_FUTURES = 1

# سقف محافظه‌کارانه اهرم پیشنهادی؛ این عدد فقط یک سقف داخلی است،
# نه تضمینی برای جلوگیری از لیکویید شدن (که به مارجین نگهداری صرافی هم بستگی دارد)
MAX_LEVERAGE = 5

# نسبت هدف سود به حد ضرر (Risk:Reward)
REWARD_RISK_RATIO = 2

# آستانه امتیاز جهت‌دار برای اینکه یک ارز فرصت لانگ/شارت شناخته شود
# (روی ۴۵ تنظیم شده تا حالت «روند تایید‌شده + هم‌سویی مومنتوم RSI» رو هم بگیرد؛
# روی ۵۵+ عملاً هیچ‌وقت در داده واقعی رخ نمی‌دهد و اسکنر همیشه خالی برمی‌گشت)
LONG_SCORE_THRESHOLD = 45
SHORT_SCORE_THRESHOLD = -45

# ---------------------------------------------------------
# ثبت تاریخچه و دقت سیگنال‌های اسکنر فیوچرز
# ---------------------------------------------------------

SIGNAL_HISTORY_PATH = "database/signal_history.json"

# اگر یک سیگنال بعد از این تعداد روز هنوز نه به حد سود نه به حد ضرر نرسیده
# باشد، به‌عنوان "منقضی" بسته می‌شود تا برای همیشه باز نماند
SIGNAL_MAX_OPEN_DAYS = 7

# ---------------------------------------------------------
# ترکیب تحلیل فاندامنتال با تحلیل تکنیکال
# ---------------------------------------------------------

# منبع شاخص ترس و طمع کل بازار کریپتو (رایگان، بدون نیاز به کلید)
FEAR_GREED_API_URL = "https://api.alternative.me/fng/"

# چند کندل اخیر برای محاسبه روند Open Interest بررسی شود
OI_TREND_LOOKBACK = 6

# وزن هر بخش در امتیاز نهایی (combined_score)؛ جمع این دو باید ۱ باشد.
# تحلیل تکنیکال (روند + RSI) وزن بیشتری دارد چون مستقیم‌تر با قیمت مرتبط
# است؛ فاندامنتال (فاندینگ/OI/ترس‌وطمع) به‌عنوان تایید/هشدار زمینه‌ای عمل می‌کند.
TECHNICAL_WEIGHT = 0.6
FUNDAMENTAL_WEIGHT = 0.4
