# هاست کردن پنل

پنل الان یه Dockerfile و docker-compose.yml داره که آماده اجرا روی هر سروریه.
دو مسیر معقول برای هاست کردنش:

## گزینه ۱: یک VPS ساده (کنترل کامل، ارزون‌تر)

مثلاً یک سرور کوچک از Hetzner، DigitalOcean یا مشابه (حتی ارزون‌ترین پلن کافیه).

1. روی سرور Docker رو نصب کن.
2. کد رو روی سرور بیار (`git clone` از یک ریپوی GitHub خصوصی، یا `scp`/`rsync`
   مستقیم پوشه پروژه).
3. `.env` رو جداگانه (نه از طریق گیت) روی سرور بذار — فایل رمز عبور هش‌شده،
   نباید جایی commit بشه.
4. `docker compose up -d --build`
5. برای HTTPS واقعی (که رمز عبور رمزنگاری‌شده رد و بدل بشه)، یک reverse proxy
   مثل Caddy جلوش بذار — Caddy با یک فایل تنظیمات ۳ خطی گواهی SSL رو خودش از
   Let's Encrypt می‌گیره و تمدید می‌کنه.

## گزینه ۲: یک پلتفرم مدیریت‌شده (ساده‌تر، HTTPS خودکار)

سرویس‌هایی مثل Railway یا Render مستقیم از روی Dockerfile دیپلوی می‌کنن و
HTTPS رو خودشون مدیریت می‌کنن؛ نیازی به تنظیم دستی سرور نیست. معمولاً یه پلن
رایگان/ارزون برای این حجم از ترافیک کافیه.

هر دو مسیر یک پیش‌نیاز مشترک دارن: کد باید جایی مثل **GitHub** باشه تا
پلتفرم بتونه بهش وصل بشه (الان این ریپو فقط لوکاله، هیچ remote نداره).

## قبل از هر دیپلوی

- `python scripts/set_password.py` رو (اگه هنوز نزدی) روی همون ماشینی که
  کانتینر رو می‌سازی اجرا کن تا `.env` ساخته بشه.
- پوشه `database/` رو به‌عنوان یک volume نگه دار (docker-compose.yml این کارو
  از قبل انجام می‌ده) وگرنه با هر دیپلوی جدید پرتفوی/تاریخچه سیگنال‌ها پاک
  می‌شه.
